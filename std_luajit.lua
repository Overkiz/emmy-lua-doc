---@param arg boolean|userdata if boolean: if true, creates a proxy obkect with a new metatable, otherwise, with no metatable. If userdata: use the metatable as the other proxy
---@return userdata
function newproxy(arg) end